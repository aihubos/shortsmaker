---
name: shortsmaker
description: 사용자 주제나 레퍼런스로 강한 오프닝 후킹이 있는 YouTube 쇼츠를 자동 구성한다. 모든 씬의 나레이션과 Google Flow 프롬프트를 먼저 출력하고, 씬 이미지와 글자가 포함된 썸네일 시안 3개를 만든 뒤 최종 YouTube 영상 제목 1개와 설명을 함께 출력하는 요청에 사용한다.
metadata:
  short-description: 쇼츠 대본·Flow·썸네일 3종 자동 완성
---

# ShortsMaker

영상 제작 텍스트를 먼저 제공한 뒤 씬 이미지, 글자가 포함된 썸네일 시안 3개, YouTube 영상 제목 1개와 설명까지 자동 완성한다. 제목 번호나 이미지 번호를 고르게 하지 않는다.

## 기본값

- 비율: `9:16`
- 길이: `120초`
- 씬: `8개`
- 30초는 4개 씬, 60초는 6개 씬
- 사용자가 이미 준 정보는 다시 묻지 않는다.
- 주제나 레퍼런스가 없을 때만 한 번에 한 가지를 짧게 묻는다.
- 주제와 길이가 있으면 중간 선택이나 승인 때문에 멈추지 않고 끝까지 진행한다.

사실형 주제는 필요한 조사를 수행하되 사용자가 요청하지 않은 조사 문서나 중간 파일을 만들지 않는다.

## 오프닝 후킹

- 영상 시작 1~3초 안에 강한 후킹을 넣는다.
- 후킹은 씬 01 나레이션의 첫 문장부터 시작한다. 인사, 채널 소개, 배경 설명을 앞에 두지 않는다.
- 주제에 맞춰 강한 질문, 예상 밖의 반전, 결과 선공개 중 가장 효과적인 방식을 하나 선택한다.
- 짧고 구체적으로 쓰며, 시청자가 다음 문장을 들어야 이유를 알 수 있는 궁금증을 만든다.
- 후킹에서 던진 질문이나 약속은 영상 본문과 결말에서 반드시 회수한다.
- 사실을 왜곡하거나 본문이 뒷받침하지 못하는 거짓 과장과 공포 표현은 사용하지 않는다.
- 씬 01 이미지와 Flow 프롬프트도 첫 순간부터 같은 후킹 상황을 시각적으로 보여준다.

## 1단계: 대본과 Flow 먼저 출력

1. 강한 오프닝 후킹, 주제 기반 작업용 프로젝트 제목, 씬별 나레이션·Flow 프롬프트·이미지 구성을 내부에서 완성한다.
2. **파일이나 이미지를 만들기 전에** 모든 씬의 텍스트를 채팅에 출력한다.
3. 각 씬은 아래 순서로 출력한다.

   - `## 씬 01`
   - `### 🎙️ **나레이션**`
   - 다음 줄에 `> ## **“해당 씬의 나레이션 원문”**` 형식의 큰 볼드 콜아웃
   - `### Flow 영상 프롬프트`와 `text` 코드블록 하나

4. Flow 프롬프트는 씬당 정확히 하나만 작성한다.
5. 모든 Flow 코드블록에 `Audio: no narration, no dialogue, no voice-over, no spoken words.`를 넣는다.
6. Flow 코드블록에는 Google Flow에 그대로 붙여넣을 영상 지시문만 넣는다. 시간, 장면 목적, 이미지 프롬프트, 파일명, 해설용 제목은 넣지 않는다.
7. 모든 씬 출력이 끝나면 마지막 `text` 코드블록에 전체 나레이션 스크립트를 순서대로 이어서 출력한다.
8. 위 채팅 출력을 끝낸 뒤에만 프로젝트 폴더와 이미지를 만든다.

## 저장 구조

현재 프로젝트 폴더 아래에 다음 최종 구조만 남긴다.

```text
Youtube/<작업용 프로젝트 제목>/
├── script.txt
├── scenes/
│   ├── 01.png
│   └── ...
└── thumbnails/
    ├── 01.png
    ├── 02.png
    └── 03.png
```

폴더는 1단계 채팅 출력이 끝난 뒤 다음 명령으로 만든다.

```bash
python3 <skill-dir>/scripts/create_project.py --title "<주제 기반 작업용 프로젝트 제목>" --root "<현재 프로젝트 폴더>/Youtube"
```

- `script.txt`: 채팅 마지막 코드블록과 같은 전체 나레이션만 저장한다.
- `scenes/NN.png`: 씬마다 첫 프레임으로 쓸 9:16 세로 이미지 1장만 저장한다.
- `thumbnails/01.png`~`03.png`: 후킹 문구가 이미 들어간 **완성 썸네일 시안 3장**을 저장한다.
- 필요하면 썸네일 배경을 내부적으로 먼저 만들 수 있지만 문구 없는 중간 이미지는 사용자에게 선택시키거나 프로젝트 폴더에 저장하지 않는다.
- 자동 확정한 YouTube 영상 제목으로 프로젝트 폴더를 이름 바꾸지 않는다.
- 같은 작업용 제목 폴더가 있으면 `-v2`, `-v3`로 새로 만들고 기존 파일을 덮어쓰지 않는다.

다음 파일과 폴더는 만들지 않는다: ZIP, SRT, 자막 파일, Flow 프롬프트 파일, 이미지 프롬프트 파일, 조사 문서, 콘셉트 문서, 타임라인 문서, YouTube 게시정보 파일.

## 2단계: 씬 이미지와 썸네일 3종 자동 완성

1. 씬 이미지를 순서대로 생성한다. 모든 씬에서 피사체·색감·장소·조명을 일관되게 유지하고 자막·로고·워터마크를 넣지 않는다.
2. 영상에서 실제로 약속하는 내용만 사용해 YouTube 영상 제목 후보 5개를 **내부에서만** 비교하고 가장 강한 1개를 자동 확정한다. 후보 목록을 보여주거나 선택을 요청하지 않는다.
3. 서로 다른 후킹 구도와 간결한 문구를 가진 **완성 썸네일 시안 3장**을 제작한다. 세 시안의 문구는 자동 확정한 영상 제목과 대본의 약속을 보완하되 서로 다른 클릭 동기를 사용한다.
4. 썸네일 문구는 전체 YouTube 제목을 억지로 넣지 않는다. 핵심 의미를 보존한 짧은 문구를 2~3줄로 쓰고, 4줄은 불가피할 때만 허용한다.

### 썸네일 글자 안전영역

- 글자 블록 전체를 이미지 가로 **8%~92%**, 세로 **24%~72%** 안에 배치한다.
- 글자 중심은 피사체에 따라 **중앙보다 살짝 위(세로 38%~45%)** 또는 **살짝 아래(세로 55%~62%)**에 둔다.
- 최상단과 최하단에는 글자를 배치하지 않는다. 글자, 외곽선, 그림자 어느 것도 안전영역 밖으로 나가면 안 된다.
- 굵은 한글 글꼴, 강한 색 대비, 검은 외곽선 또는 그림자를 사용하고 좌우 여백을 유지한다.
- 인물의 얼굴·손, 제품, 망원경, 로켓처럼 핵심 피사체를 가리지 않는다. 피사체가 중앙을 차지하면 글자 블록을 위·아래 중앙 안전영역 중 빈 곳으로 옮긴다.
- 문구가 길면 글자 크기를 무리하게 줄이거나 화면 끝으로 밀지 말고 문구 자체를 짧게 다듬는다.

5. 생성된 3장을 원본 크기와 390px 축소 화면에서 직접 확인한다. 한글 철자, 문장부호, 잘림, 가독성, 비율, 핵심 피사체 보존을 확인하고 문제가 있는 이미지만 수정한다.
6. 세 시안을 `thumbnails/01.png`~`03.png`로 저장하고 채팅에 모두 보여준다.
7. 영상에서 실제로 다룬 내용만 사용해 복사해 붙여넣을 수 있는 **YouTube 영상 설명**을 작성한다.
8. 썸네일 3장을 보여준 뒤 YouTube 영상 제목과 설명을 각각 별도의 복사용 `text` 코드블록으로 출력한다.

````markdown
### YouTube 영상 제목

```text
자동 확정한 YouTube 영상 제목 1개
```

### YouTube 영상 설명

```text
문제와 영상의 가치를 전하는 첫 두 줄

영상에서 보여주는 내용 2~4문장

짧은 행동 유도

#핵심해시태그 #3개에서6개
```
````

설명은 제목과 대본의 약속을 벗어나지 않아야 한다. 타임스탬프, 출처, 상표, AI 생성 콘텐츠 고지는 실제로 필요한 경우에만 넣는다. YouTube 영상 제목과 설명은 채팅에만 출력하고 별도 게시정보 파일을 만들지 않는다.

실제 YouTube 게시나 외부 전송은 사용자가 따로 요청한 경우에만 한다.

## 최종 완료 출력 형식

모든 이미지와 썸네일 작업이 끝나면 최종 완료 보고에서도 승인된 씬별 텍스트를 생략하지 말고 **가장 위에 다시 출력**한다. 최종 보고만 읽어도 대본과 Flow 프롬프트를 모두 복사할 수 있어야 한다.

최종 출력 순서는 다음과 같다.

1. 작업용 프로젝트 제목과 비율·길이·씬 수
2. `씬 01`부터 마지막 씬까지의 나레이션과 Flow 영상 프롬프트
3. 전체 나레이션 `text` 코드블록
4. `최종 완료 보고`
5. 결론, 완료한 일, 최종 썸네일 3장, 실제 확인 결과
6. YouTube 영상 제목 `text` 코드블록
7. YouTube 영상 설명 `text` 코드블록

최종 완료 보고에는 `막힌 점`과 `다음 행동` 항목을 작성하지 않는다.

최종 보고의 씬별 나레이션도 아래 형식을 반복한다.

````markdown
## 씬 01

### 🎙️ **나레이션**

> ## **“해당 씬의 나레이션 원문”**

### Flow 영상 프롬프트

```text
Google Flow에 그대로 붙여넣을 프롬프트
```
````

- 나레이션은 작은 일반 문단으로 표시하지 않는다. 마크다운 인용문 안의 2단계 제목과 볼드를 함께 사용해 크게 보이게 한다.
- Flow 프롬프트는 항상 `text` 코드블록을 사용한다. `yaml`, `sql`, `perl`처럼 내용과 관계없는 언어 태그를 사용하지 않는다.
- 1단계에서 확정한 씬별 나레이션과 Flow 프롬프트를 최종 보고에서 줄이거나 바꾸지 않는다.
- YouTube 영상 제목 코드블록에는 제목만 넣고, 영상 설명 코드블록에는 설명 본문과 해시태그만 넣는다.

## 완료 확인

- 씬 수와 Flow 코드블록 수가 같은가
- 씬 01의 첫 문장이 1~3초 안에 전달되는 강한 후킹이며 앞에 인사나 설명이 없는가
- 후킹의 질문이나 약속이 본문과 결말에서 회수되는가
- 모든 씬에 나레이션과 Flow 프롬프트가 있는가
- 모든 Flow 프롬프트에 음성·대화·나레이션 금지 문장이 있는가
- 전체 스크립트가 마지막 코드블록에 한 번만 나오는가
- 채팅 텍스트 출력 전에 이미지 생성을 시작하지 않았는가
- 제목 번호나 이미지 번호 선택을 요청하지 않고 끝까지 자동 완성했는가
- 썸네일 3장 모두 글자가 포함된 완성 시안인가
- 모든 글자 블록이 가로 8%~92%, 세로 24%~72% 안전영역 안에 있는가
- 글자가 최상단이나 최하단에 닿거나 잘리지 않았는가
- 원본 크기와 390px 축소 화면에서 한글 철자와 가독성을 확인했는가
- 최종 썸네일 3장과 YouTube 영상 제목·설명을 함께 출력했는가
- 최종 완료 보고의 가장 위에 모든 씬의 나레이션과 Flow 프롬프트를 다시 출력했는가
- 씬별 나레이션을 큰 볼드 콜아웃 형식으로 표시했는가
- YouTube 영상 제목과 영상 설명을 각각 별도의 `text` 코드블록으로 출력했는가
- 최종 완료 보고에 `막힌 점`이나 `다음 행동` 항목이 포함되지 않았는가
- 저장 파일이 `script.txt`, 씬별 이미지 1장, 문구가 들어간 최종 썸네일 3장뿐인가
